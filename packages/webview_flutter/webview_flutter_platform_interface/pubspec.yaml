name: webview_flutter_platform_interface
description: A common platform interface for the webview_flutter plugin. Solflare fork with ProcessTerminationDetails.
repository: https://github.com/solflare-wallet/packages/tree/main/packages/webview_flutter/webview_flutter_platform_interface
issue_tracker: https://github.com/solflare-wallet/packages/issues
# NOTE: We strongly prefer non-breaking changes, even at the expense of a
# less-clean API. See https://flutter.dev/go/platform-interface-breaking-changes
version: 2.11.0
publish_to: none

environment:
  sdk: ^3.4.0
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter
  meta: ^1.7.0
  plugin_platform_interface: ^2.1.7

dev_dependencies:
  build_runner: ^2.1.8
  flutter_test:
    sdk: flutter
  mockito: ^5.4.4

topics:
  - html
  - webview
  - webview-flutter
